# Portable ForceSAPIEN definitive package

Serve this directory with a local HTTP server and open `index.html`. Each object directory contains exactly `final_video.mp4`, `simulation.json`, `contact_sheet.png`, and `run.log`. Camera framing uses only swept visual-link bounds; overlays never affect camera bounds.
